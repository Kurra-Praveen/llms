package com.loanplatform.loan.entity;

public enum InterestType {
    FLAT,
    REDUCING_BALANCE,
    SIMPLE,
    INTEREST_ONLY,
    BULLET,
    DAILY_FIXED  // Fixed rupee amount per day, irrespective of principal
}
