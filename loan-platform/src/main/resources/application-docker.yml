spring:
  config:
    activate:
      on-profile: docker

  datasource:
    url: jdbc:postgresql://postgres:5432/loan_platform
    username: postgres
    password: postgres

  data:
    redis:
      host: redis
      port: 6379

logging:
  level:
    com.loanplatform: INFO
    org.hibernate.SQL: INFO
